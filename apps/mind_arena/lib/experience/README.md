# Experience boundary

Owns application-level orchestration, state transitions, interaction commands, facilitation,
reflection, progress, and restoration. It depends on domain contracts and must not import concrete
data sources, platform SDKs, widgets, or model-provider clients.
