# Domain boundary

Owns pure Dart models, repository contracts, value objects, validation, and policies. Domain code
must remain independent of Flutter, UI, data implementations, platform services, analytics, and
model providers.
