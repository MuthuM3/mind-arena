# Offline boundary

Owns local availability, recovery, pending-action, cache-state, and synchronization-status
abstractions when an approved slice needs them. It must preserve provenance and cannot represent
cached or editorial content as fresh human participation.
