# Shared foundation boundary

Owns small, explicitly named error, result, and privacy-safe logging primitives after more than one
approved consumer needs them. It is not a `utils` dumping ground, and feature-specific behavior
must remain with its owner.
