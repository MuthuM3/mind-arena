# Component boundary

Owns reusable accessible primitives, controls, feedback, arena, and reflection widgets. Components
receive state and callbacks; they must not call repositories, own arena rules, or import concrete
data implementations.
