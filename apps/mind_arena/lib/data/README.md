# Data boundary

Owns mappings and concrete mock or local implementations of domain repository contracts. M1.5 is
backend-free: no API client, production database, or cloud SDK belongs here. A network adapter is
added only in a separately approved connected milestone.
